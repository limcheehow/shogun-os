# ── Coding Agent Profile Config ──
# Optimized for code generation tasks
# Generated from templates/profiles/coding-config.yaml
# Uses the default Hermes model config.

model:
  default: deepseek-v4-flash
  provider: custom

fallback_providers: []

providers: {}

mcp_servers:
  gbrain:
    command: gbrain
    args: [mcp]