# Chatwoot .env — generated by Shogun OS Kizuna onboarding
# Copy to docker-compose.yml directory and edit as needed

# Required: Generate with: openssl rand -hex 64
SECRET_KEY_BASE=change-me

# Public-facing URL (set after you have a domain + reverse proxy)
FRONTEND_URL=http://localhost:3000

# Postgres password (change me)
POSTGRES_PASSWORD=changeme